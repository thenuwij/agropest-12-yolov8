# Core
ultralytics>=8.1.0,<9
torch>=2.0
torchvision>=0.15

# Data / metrics / plotting
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
opencv-python-headless>=4.8
PyYAML>=6.0

# Dataset download (optional — only needed for `python -m agropest.data`)
kagglehub>=0.2

# Interpretability (optional — Grad-CAM analysis)
grad-cam>=1.4
